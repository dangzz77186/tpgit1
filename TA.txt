Je me suis occupé de la création de l'index.

Les commandes utilisées sont : 
mkdir ControleGIT
cd ControleGIT

git init

git remote add origin "https://github.com/dangzz77186/controle-git.git"

git add TA.txt

git config user.name "dangzz77186"
git config user.email "dang-luan.ta@efrei.net"

git commit -m "premier commit; document recap"

git push -u origin master

git branch dangluan

git checkout dangluan

git checkout master

git merge dangluan
